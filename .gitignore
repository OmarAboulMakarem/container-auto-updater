# Secrets
.env
.env.local
.env.production
.env.staging
*.local

# Internal tooling / plans — not relevant to public users
docs/
.claude/

# Internal CI (GitLab-specific, not needed on GitHub)
.gitlab-ci.yml
